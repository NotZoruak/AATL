# 合战场（Sortie）任务设计

## 概述

- 流水线文件：`resource/base/pipeline/Sortie.json`
- 入口节点：`Sortie`
- 主枢纽：`S_DetectWhereAmI`
- 节点前缀：`S_`
- 总节点数：197

## GUI 选项

| 选项 | 类型 | 说明 |
|------|------|------|
| S_选择时代 | select | 维新的记忆～延享的记忆 |
| S_选择地域 | select | 各地域地图 |
| S_选择部队 | select | 部队一 ～ 五 |
| S_选择阵形 | select | 各阵形 |
| S_重伤处理 | select | 修刀 / 停止 / 撤退，默认修刀 |
| S_刀装破坏处理 | select | 停止 / 补充刀装 / 刀装保护 / 补充+刀装保护，默认补充刀装 |
| S_疲劳处理 | select | 无 / 刷花 / 停止，默认无 |
| S_换队长 | checkbox | 切换队长方式 |
| S_王点前撤退 | checkbox | 到达王点前撤退 |
| S_道中撤退 | checkbox | 道中特定点撤退 |
| S_同步远征 | checkbox | 回本丸后自动派遣远征 |

## 主枢纽 `S_DetectWhereAmI`

25 个检测节点，超时 120s，`on_error → S_RestartGame`：

```json
"S_DetectWhereAmI": {
    "pre_delay": 0,
    "timeout": 120000,
    "on_error": ["S_RestartGame"],
    "next": [
        "S_IsAnnouncementPopup", "S_IsLoginReward",
        "S_HasExpeditionReturn_Exp", "S_IsMarching",
        "S_IsBattleResult_Exp", "S_IsSwordDropColor",
        "S_IsKebiishi", "S_IsFormationSelect",
        "S_IsSwordSelect", "S_IsTeamSelect",
        "S_IsEraSelect", "S_IsRegionSelect",
        "S_IsSortieActivity", "S_IsMenuDirectory",
        "S_IsTeamStatusPanel", "S_CheckHomeBrightness",
        "S_IsRepair", "S_IsStopOnDamage",
        "S_IsGameIcon", "S_IsLoginButton",
        "S_IsGameUpdatePopup", "S_IsInGameUpdatePopup",
        "S_IsInternalReport",
        "S_IsTroopRecordPage", "S_FallbackWait"
    ]
}
```

`S_IsTroopRecordPage` TemplateMatch `部队记录页面.png`（ROI `[577,8,130,35]`，阈值 0.95），命中后点击 `[63,28,17,17]` 返回枢纽。

## 主流程

### 出阵准备

```
本丸 → S_CheckHomeBrightness（`next: ["S_IsHome", "S_DetectWhereAmI"]`）→ S_IsHome → S_NavigateToSortie → S_ClickMenu
  → S_PostClickMenuHub → S_VerifyMenuSortie / S_CheckNavDirForRetry / S_DetectWhereAmI
    → S_ClickSortieInMenu（OCR "出阵"）→ S_VerifyMenuClosed → S_TryClickSortie
```

### 时代/地域/部队选择

`S_IsEraSelect` → `S_ClickEra` → `S_ConfirmEra`（时代选择）。`S_IsRegionSelect` → `S_VerifyRegionSelect` → `S_ClickRegion`（地域选择）。`S_IsTeamSelect` → `S_ClickTeam`（部队选择，由 GUI 选项覆盖 target 坐标）。

### 刀装补充

`S_CheckEquipmentPopup` TemplateMatch `整备刀装.png` → `S_PreCheckTroopRecord` → `S_PreCheckUseRecord` → `S_PreCheckClickRecordN` → `S_PreCheckUseRecordBtn`。

该链路由 GUI 选项 `S_刀装破坏处理` 选择「补充刀装」或「补充+刀装保护」时启用（pipeline_override 开启 `S_CheckEquipmentPopup` 等节点）。

### 换队长

`S_CaptainHub` → `S_DragCaptain`（Custom 拖拽）/ `S_IsPreSortieConfirm` → `S_ClickSortieNow`。

### 行军循环

```
S_PostSortieHub
  ├─ 重伤检测（S_IsPreDamage1/2, S_StopOnDamagePopup/Text）
  ├─ 刀装弹窗（S_CheckEquipmentPopup）
  └─ S_DetectAfterSortieNow
       ├─ S_IsTroopDepleted（刀装破损）
       ├─ S_IsMarching → [S_IsMarchingDamage, S_FatigueDetect, S_EqCheck, S_CheckBoss, S_PostClickMarching]
       │    ├─ S_IsMarchingDamage（行军中重伤）
       │    ├─ S_FatigueDetect（行军中重疲劳）
       │    ├─ S_EqCheck（刀装保护路由）
       │    ├─ Boss 检测（S_Boss_E2~8_R* 等 34 节点）
       │    ├─ MidRetreat 节点（道中撤退，5 节点）
       │    └─ S_PostClickMarching（兜底前进）
       ├─ S_IsFormationSelect → 阵形选择
       ├─ S_FallbackWait
       └─ S_DetectWhereAmI
```

`S_IsMarching` 的 next 已扁平化，不再经子枢纽转发，按上述顺序直接串联检测。各节点说明：

| 节点 | 类型 | 识别 | 动作 | 说明 |
|------|------|------|------|------|
| `S_IsMarchingDamage` | TemplateMatch | 行军中重伤 | 撤退 | 行军重伤检测 |
| `S_FatigueDetect` | TemplateMatch | `Common/重疲劳.png`，ROI `[254,154,31,502]`，阈值 0.9 | Click `[763,446,48,44]` | 行军中检测到重疲劳 → 回本丸 |
| `S_EqCheck` | DirectHit 路由 | — | — | 刀装保护路由枢纽，next 含模板/颜色检测节点与 `S_CheckBoss`、`S_PostClickMarching` 兜底 |
| `S_EqCheckTemplate` | TemplateMatch | `Common/刀装破坏.png`，ROI `[576,135,162,529]`，阈值 0.85 | Click `[763,446,48,44]` | 刀装已被破坏 → 回本丸 |
| `S_EqCheckC_R0C0` ~ `S_EqCheckC_R5C2`（18 个） | ColorMatch | 单像素点颜色 `[183,182,182]`±1，3 列 × 6 行（列 x 591/650/708，行 y 自 184 递增） | Click `[763,446,48,44]` | 刀装接近破坏 → 回本丸 |
| `S_CheckBoss` | 路由 | — | — | 王点/道中撤退路由（原名 `S_CheckBossBeforeMarch`） |
| `S_PostClickMarching` | Click | — | Click `[1072,563,110,69]` | 兜底前进 |
| `S_ReturnHomeFromMarching` | — | — | — | 行军中撤退回本丸（原名 `S_ReturnHomeAfterMarchDamage`） |

`S_CheckBoss` 路由 39 个检测节点，covering 所有时代王点和道中撤退场景，末尾接 `S_PostClickMarching` 兜底。`S_FatigueDetect`、`S_EqCheck` 及其下级节点默认 `enabled: false`，由 GUI 选项 `S_疲劳处理`、`S_刀装破坏处理`（刀装保护 / 补充+刀装保护）通过 pipeline_override 启用。

### 重伤处理

| 模式 | 节点链 | 说明 |
|------|--------|------|
| 修刀 | `S_IsPreDamage1/2` → `S_LogPreDamage*` → `S_IsRepair` → `S_ClickRepair` → `S_ConfirmRepair` | 修刀后 `S_ReturnHome` → `S_ClickHomeInMenu` → 回本丸 |
| 停止 | `S_StopOnDamagePopup/Text` → `S_LogStopOnDamage*` | 弹窗检测，停止任务 |
| 行军重伤 | `S_IsMarchingDamage` → `S_LogMarchingDamage` → `S_ReturnHomeFromMarching` → `S_ConfirmReturnHome` → `S_ReturnHome` | 行军中检测到重伤立即撤退 |
| 行军重疲劳 | `S_FatigueDetect` → `S_ReturnHomeFromMarching` → `S_ConfirmReturnHome` → `S_ReturnHome` | 行军中检测到重疲劳回本丸 |
| 刀装破坏 | `S_EqCheck` → `S_EqCheckTemplate` / `S_EqCheckC_R*C*` → `S_ReturnHomeFromMarching` → `S_ConfirmReturnHome` → `S_ReturnHome` | 行军中刀装被破坏或接近破坏，回本丸保护刀装 |

`S_ConfirmReturnHome` TemplateMatch `合战场_确认返回本丸.png`（ROI `[564,305,152,33]`，阈值 0.9）。

### 王点撤退

`S_ClickRetreat` → `S_ConfirmRetreat`（OCR "确认返回本丸"）→ `S_ReturnHome`。

### 回到本丸

```
S_ReturnHome → S_ClickMenuFromRepair（OCR "目"）→ S_ClickHomeInMenu（点击 [1037,169,110,44]）→ 回本丸
或：S_ClickMenuStop → S_ClickHomeStop → S_ClickHomeFallback
```

`S_ClickHomeMenu` 使用 TemplateMatch `目录.png`，`S_CheckNavDirForRetry` OCR "目"（ROI `[1183,18,87,40]`）兜底重试。

## 导航链路

`S_ClickMenu` OCR "目" → `S_PostClickMenuHub` → `S_VerifyMenuSortie` / `S_CheckNavDirForRetry` / `S_DetectWhereAmI`。

`S_ClickSortieInMenu` OCR "出阵" → `S_VerifyMenuClosed`（TemplateMatch 菜单目录.png）→ `S_TryClickSortie` → `S_ClickSortieInMenu` 重试 / `S_DetectWhereAmI` 兜底。

## 闪退恢复

| 节点 | 识别 | 动作 |
|------|------|------|
| `S_IsAnnouncementPopup` | TemplateMatch `公告弹窗.png` | 自循环关闭 |
| `S_IsLoginReward` | TemplateMatch `登录奖励.png` | 三击关闭 |
| `S_HasExpeditionReturn_*` | OCR | 点击关闭远征归来弹窗 |
| `S_IsGameIcon` | TemplateMatch `游戏图标.png`（全屏） | 点击重进 |
| `S_IsLoginButton` | TemplateMatch `登录.png` | 点击登录 |
| `S_IsGameUpdatePopup` | TemplateMatch `游戏更新.png` | 关闭弹窗 |
| `S_IsInGameUpdatePopup` | OCR "更新" | 关闭弹窗 |
| `S_FallbackWait` | DoNothing | 回枢纽 |

## 重启游戏

```json
"S_RestartGame": {
    "action": { "type": "Custom", "custom_action": "RestartGameAction" },
    "next": ["S_DetectWhereAmI"]
}
```

## on_error 覆盖

- 主枢纽 next 列表中节点不设 on_error
- 主枢纽 `on_error → S_RestartGame`
- `Sortie`（入口）、`S_ReturnHome`、`S_CheckAccel`、`S_IsHome` 无需（审核通过）
- `S_DetectSortOrder`、`S_DecideDamage_Pre`、`S_CaptainHub`、`S_TryClickRepair`、`S_PostSortieHub`、`S_PostClickMenuHub`、`S_SelectDamagedSword` 等路由节点已配置 `on_error → S_DetectWhereAmI` 或专用路由
- `S_ClickDescending`、`S_ConfirmSortAsc`、`S_ClickRepairDone`、`S_ClickMenuFromRepair`、`S_ClickMenu` 等识别节点已配置 `on_error → S_DetectWhereAmI`
- `S_TryClickRepair → S_SelectDamagedSword` on_error 指向 `S_CheckRepairScreen`（修复流程路由）

## 设计要点

- **部队记录页面防御**：`S_IsTroopRecordPage` 防止误触卡死
- **目录识别**：使用 OCR "目" 点击目录按钮
- **行军后子枢纽**：`S_DetectAfterSortieNow` 集中处理行军中的弹窗和状态变化，`S_IsMarching` 扁平化串联重伤/疲劳/刀装/王点检测
- **重伤三段处理**：出战前（修刀/停止）、行军中（撤退）、确定出阵前，全覆盖
- **Boss/MidRetreat 路由**：`S_CheckBoss` 39 检测节点覆盖所有时代，末尾 `S_PostClickMarching` 兜底，无需逐个加 on_error
- **刀装破坏处理**：`S_刀装破坏处理` 下拉框组合启用刀装补充链路（`S_CheckEquipmentPopup` 系列）与刀装保护链路（`S_EqCheck` 系列，行军中检测刀装被破坏/接近破坏即回本丸）
- **刀装修复链路**：通过 `interface.json` 修刀选项启用整条 `S_ClickRepair` → `S_ConfirmRepair` → `S_ClickMenuFromRepair`
- **同步远征**：`S_IsHome.next` override 为 `["Expedition"]`
