# 后续优化规划

> 创建日期：2026-07-17

## GUI 交互

### 复选框改造（已完成 2026-07-17）

- [x] 代码：`TaskOptionGenerator.CreateCheckboxControl` 中 `ToggleButton` → `CheckBox`
  - 单 case：`CheckBox` + `Content` 绑选项 label
  - 多 case：`CheckBox` + `Content` 绑 case 名称，统一等宽
  - 布局：多 case 用 `WrapPanel` 替代 `UniformGrid`，支持自适应换行
- [x] 样式：继承 SukiUI `CheckBoxStyles.axaml` 的 GlassCard 模板，与任务列表勾选框一致
- [x] 发布流程记录：`dotnet publish` 后需手动拷贝 `MFAAvalonia.Core.dll`、`SukiUI.dll` 到 `runtimes/libs/`，拷贝 `MATR.exe`、`MATR.dll` 到根目录

## 待讨论

（待定）

---

## 已确定

（待定）
