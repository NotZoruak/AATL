# 已隐藏任务（待活动开放后恢复）

以下任务定义已从 `interface.json` 中移除，需要时恢复。

## 地下城

```json
{
  "name": "地下城",
  "entry": "Underground",
  "default_check": false,
  "repeatable": true,
  "repeat_count": 1,
  "option": [
    "U_层数选择",
    "U_重伤处理",
    "U_选择部队",
    "U_选择阵形",
    "U_每轮回本丸",
    "U_换队长",
    "U_补充刀装",
    "U_同步远征",
    "U_动画跳过"
  ],
  "label": "地下城",
  "description": "地下城活动自动化出阵。\n\n- **层数选择**：设定目标层数（1-99）。须同时启用「每轮回本丸」\n- **每轮回本丸**：打完一轮后自动返回本丸。不启用时无法正确计算重复次数，建议此时设置为重复999次\n- **重伤处理**：选择修刀则在出阵前或行军中检测到重伤后自动修复；选择停止则检测到重伤后终止任务\n- **换队长**：通过筛选装备马的刀剑来切换队长，请为且仅为需控花的刀剑男士装备马\n- **补充刀装**：出阵前检测刀装不足弹窗并自动补充，使用前请保存部队记录至记录一\n- **同步远征**：回本丸后自动检查队伍状态并派遣远征。请确保地下城使用的队伍在远征任务中设置为「休息」\n- **动画跳过**：增加点击，提高挖地效率"
}
```

