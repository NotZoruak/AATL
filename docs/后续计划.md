# AATL 后续计划

> 记录想做的优化和新功能，不作为开发承诺，仅备忘。

## 待优化

- ~~行军中重伤检测改用模板匹配~~ ✅ 已完成（2026-06-28）：`S_IsMarchingDamage2` / `U_IsMarchingDamage2` 从 OCR 改为 TemplateMatch，ROI `[229, 116, 90, 569]`，模板 `重伤.png`，阈值 0.85，green_mask。命中后直接 Click 关闭弹窗，日志后走 `ReturnHomeAfterMarchDamage`
- ~~修刀完成后输出日志~~ ✅ 已完成（2026-06-28）：`S_ClickRepairDone` / `U_ClickRepairDone` 后插入 `LogRepairDone` 节点，输出"[重伤检测] 修复完成"
- **彻底终止循环任务**：重伤停止模式目前只能停当前轮，repeatable 任务会被 GUI 重新拉起。需探索 GUI 层 API 或等待 MaaFramework 支持

## 新功能想法

### 新任务流水线

- **习合**（习合搓糖）🔄 开发中（2026-06-30）：流水线 Mix.json 已创建，调试中。自动习合 + 搓糖模式（7级跳过），五个位置逐位检测

### 侧边栏工具

- **数据查找表** ✅ 已完成（2026-06-29）：经验表（极）含 8 刀种极化数据与升级计算器，远征收益含 20 条真实数据、效率着色与行选中。锻刀公式与地图收益已隐藏，待后续补充真实数据后启用
