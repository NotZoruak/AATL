# AATL 开发日志

## v0.5.1 热修复 — 重伤检测与修刀流程修复（2026-06-19 晚间）

### 重伤弹窗检测与关闭
- `U_IsPreDamage1` 及同类节点（`S_IsPreDamage1`、`U_StopOnDamagePopup`、`S_StopOnDamagePopup`）模板匹配阈值 0.98→0.9，解决换电脑后分辨率差异导致得分偏低的问题
- `U_ClickSortieNow.next` 在 `U_ConfirmEnterUnderground` 之前增加 `U_IsPreDamage1`、`U_IsPreDamage2`，出阵前先检测重伤弹窗
- `U_IsPreDamage1` 点击动作从模板匹配位置改为固定坐标 `target: [772, 448, 32, 40]` + `offset: [4, 2, 0, 0]`，点击弹窗"否"按钮关闭弹窗后再进修刀，避免弹窗阻挡后续菜单操作

### 地下城入口循环卡死修复
- `U_EnterUnderground` 增加 `pre_delay: 500`、`post_delay: 1500`，参照合战场 `S_ClickRegion`（`pre_delay: 500`、`post_delay: 800`），等待游戏页面过渡动画完成后再检测，避免检测到旧页面形成死循环

### 修刀完成后菜单点击修复
- `U_DirAfterRepair` 的 `pre_delay` 从 100ms 改为 500ms，等待修刀完成画面过渡后再点击"目录"按钮打开菜单

### 启动自动清理
- `AppPaths.cs` 新增 `CleanupOldDebugLogs` 方法：启动时删除 `debug/` 下超过 3 天的 `maafw.bak.*.log` 备份日志
- 扩展 `CleanupObsoleteExecutableBackups` 至 `libs/` 子目录，清理 DLL 自动更新旧备份（每个 DLL 仅保留最新一份）
- `App.axaml.cs` 启动流程中调用以上清理
- 重新编译 AATL.exe / AATL.dll / MFAAvalonia.Core.dll

### 重新打包
- 重新打包并上传 v0.5.1 release zip，替换旧发布包

## v0.5.1 热修复 — 地下城出阵确认与误触修复（2026-06-19）

### 即刻出阵确认弹窗修复
- `U_ConfirmEnterUnderground`：OCR "地下" → TemplateMatch `出阵确认.png`，ROI `[576, 60, 127, 33]`
- 游戏 UI 更新后 OCR 无法匹配"地下"文字（实际返回"地机""万御机线"），改为模板匹配后得分 1.0 稳定命中

### 合战场活动入口误触即刻出阵修复
- `U_IsEventPage` → `U_VerifyEventPage`（二次确认）→ `U_EnterUnderground`
- 参照合战场 `S_IsRegionSelect → S_VerifyRegionSelect → S_ClickRegion` 二次确认模式
- 修复前进入地下城活动页面时，`U_EnterUnderground` 连续触发两次，第二次点击位置与"即刻出阵"重叠，在部队选择页误触出阵按钮，绕过部队选择直接出阵且导致 `U_PreConfirmSupply` 死胡同
- `U_IsEventPage` 和 `U_VerifyEventPage` 均添加 `on_error: U_DetectWhereAmI`

### 重新打包
- 重新打包并上传 v0.5.1 release zip，替换旧发布包

### 分辨率适配说明
- `interface.json` 中 `display_short_side: 720` 将模拟器截图统一缩放至短边 720 像素，确保不同分辨率下 ROI 坐标一致
- 实测模拟器原生 1280×720 可正常运行，减少缩放步骤，模板匹配更稳定
- README 更新：推荐 MuMu 模拟器 12 + 1280×720，说明其他 ADB 模拟器及分辨率亦可运行

### 待办：习合
- 习合（合成强化）自动化任务，待后续开发

## v0.5.1 热修复 — 版本号、文档与许可协议（2026-06-19）

### 版本号修复
- `interface.json` 版本号 0.5.0 → 0.5.1，GUI 现已正确显示资源版本
- 重新打包并发布 v0.5.1 prerelease，替换旧发布包

### README 更新
- 任务说明新增地下城和回到本丸任务
- 下载方式新增展开 Assets 的操作指引
- 目录结构补全 Underground.json 和 GoHome.json

### 许可协议
- 新增 [GNU General Public License v3.0](LICENSE) 协议文件
- README 声明中注明基于 [MaaFramework](https://github.com/MaaXYZ/MaaFramework)（LGPL v3）构建

### 打包脚本
- `pack.sh` 排除列表移除已删除的 sample.json

## v0.5.1 — 地下城流水线完善（2026-06-18）

### 行军中重伤检测
- `U_DetectAfterSortieNow` 新增 `U_IsMarchingDamage2`，在地下城内部行军时检测重伤弹窗
- OCR "重伤"（ROI `[469, 297, 49, 26]`）→ 点击"否"取消 → 返回本丸 → 确认弹窗 → 回主枢纽
- 修复前该节点虽已定义但未被引用（死节点），行军中炸刀装重伤无法处理

### 重伤停止机制修复
- `U_StopOnDamageText` 补全 OCR 参数（`roi: [465, 310, 46, 24], expected: "重伤"`）
- 修复前缺少 roi 和 expected 导致启用后无条件停止，不检测实际重伤状态
- Sortie 中 `S_StopOnDamageText` 同样修复

### 死节点清理
- 删除 `U_ConfirmCancelPreDamage1`：`U_IsPreDamage1` 模板匹配直接点击弹窗关闭，不需要额外"否"点击
- 删除 `S_ConfirmCancelPreDamage1`：同上
- 删除 `U_VerifyLeftMenuAfterRepair`：修刀流程重构后遗留的孤立菜实验证节点
- 删除 `S_VerifyLeftMenuAfterRepair`：同上

### 修刀导航修复
- `U_DecideDamage_Pre` 从直接走 `U_ClickMenuForRepair` 改为先走 `U_NavigateToRepair`
- `U_NavigateToRepair` 提供 `post_delay: 300ms` 缓冲和 `U_DetectWhereAmI` 兜底，修刀前 UI 更稳定
- Sortie 中 `S_DecideDamage_Pre` 同样修复

### 同步远征恢复
- 地下城任务重新加入 `U_同步远征` 复选框选项
- `U_IsHome` 路由到远征入口，远征全部忙碌时回地下城导航
- `U_WaitRefresh` 重新被引用，不再死节点
- GUI 源码 `MaaProcessor.cs`：同步远征配置共享从仅 Sortie 扩展至 Sortie + Underground
- `o.Name` 匹配改为 `EndsWith("同步远征")`，兼容前缀命名
- 重新编译 AATL.exe / AATL.dll / MFAAvalonia.Core.dll

### 轮次计数与每轮回本丸
- 新增 `U_CheckRoundComplete`：OCR "传送凭证"（ROI `[947, 148, 101, 34]`）
- 行军检测到 `合战场_行军.png` 后先识别传送凭证，识别到则本轮完成
- 新增 `U_每轮回本丸` 复选框：
  - 不勾选：识别到传送凭证 → 任务终止 → GUI repeat_count 重启下一轮
  - 勾选：识别到传送凭证 → `U_GoHomeAfterRound` 点击 `[1208, 449]` 回本丸 → 任务终止 → 计数
- 任务改为 `repeatable: true, repeat_count: 1`，用户可在 GUI 自由调整轮次
- 选项顺序：重伤处理 → 选择部队 → 选择阵形 → 换队长 → 补充刀装 → 每轮回本丸 → 同步远征 → 动画跳过

### 轮次检测修复
- `U_IsMarching.next` 从仅 `[U_CheckRoundComplete]` 改为 `[U_CheckRoundComplete, U_ClickMarching]`
- MaaFramework next 列表中子节点识别失败不触发其 on_error，而是触发上级 next 备选机制
- 修复前仅一项导致死循环不点行军，现在传送凭证未识别时自动顺延点行军

### 回本丸确认
- `U_GoHomeAfterRound` 点击 `[1208, 449]` 回本丸后 `next` 走 `U_ConfirmReturnHome`，复用行军重伤回城确认节点
- 删除了重复的 `U_ConfirmRoundHome`，`U_每轮回本丸` override 改为设置 `U_ConfirmReturnHome.next = []` 终止任务

### 内番报告检测
- 六个有闪退恢复的流水线新增 `*_IsInternalReport` 主枢纽节点
- 模板匹配 `内番报告.png`，ROI `[569, 322, 138, 31]`，识别后点击关闭回主枢纽
- Expedition 流水线修复：`E_IsInternalReport.next/on_error` 指向 `Expedition` 而非不存在的 `E_DetectWhereAmI`

### 主枢纽顺序调整
- 地下城主枢纽 `U_CheckHomeBrightness` 挪到 `U_IsMenuDirectory` 之前
- 回本丸后优先检测本丸 → `U_IsHome` → 同步远征，防止菜单目录抢先匹配跳过远征

## v0.5.0 — 地下城活动实装 + 自动更新 + 历史整理（2026-06-18）

### 地下城活动流水线实装
- Underground.json 从 97 节点骨架完善至 100 节点
- `U_IsEventPage`：更新地下城.png 识别 ROI `[427, 69, 388, 52]`，命中后 `U_EnterUnderground` 点击 `[1141, 614, 120, 49]` 进入
- 新增 `U_ConfirmEnterUnderground`：OCR"地下"（ROI `[550, 145, 79, 32]`）检测确认弹窗，点击 `[706, 582, 154, 42]` 进入部队选择
- 新增 `U_IsTutorial`：模板匹配 `地下城教程.png`（ROI `[856, 387, 35, 30]`），跳过教学引导
- `U_IsSpecialDrop`：更新 `特殊掉落_地下城.png` 识别 ROI `[1044, 29, 166, 87]`
- `U_ClickUndergroundInMenu`：补全 OCR"出阵"→点击，菜单中导航到合战场
- 新增 `U_动画跳过` 复选框：勾选后 `U_FallbackWait` 点击 `[180, 11, 25, 28]` 代替 DoNothing，加速动画
- 移除同步远征选项（地下城期间不回本丸）
- 任务描述仅保留换队长、补充刀装、动画跳过三项
- 模板图片新增：`地下城.png`、`地下城教程.png`、`特殊掉落_地下城.png`

### 自动更新配置
- `interface.json` 新增 `Github` 字段，指向 `NotZoruak/AATL`
- 修改源码 `DownloadSourceIndex` 默认值从 1 (MirrorChyan) 改为 0 (GitHub)
- 重新编译 AATL.exe / AATL.dll / MFAAvalonia.Core.dll
- GUI 更新设置：下载源默认 GitHub，更新通道稳定版，自动检测开启
- 修复 GitHub API 限速(403/429)时弹错误 toast 问题：限速异常静默记录 Warning，不再打断用户

### 死节点清理
- 删除 Sortie.json 死节点 `S_IsMarchingDamage1` 和 `S_DecideDamage_March`
- 删除 Underground.json 对应死节点 `U_IsMarchingDamage1` 和 `U_DecideDamage_March`

### 任务说明精简
- 合战场和地下城的任务描述移除重伤处理相关说明
- 地下城移除王点前撤退、八图道中撤退、同步远征描述

### Git 历史整理
- 56 个提交 squash 为 1 个干净的初始提交
- 所有 v0.x Release（v0.1.1 ~ v0.5.0）标记为 prerelease（内测版本）
- 旧历史保留在 backup-20260618 备份分支

## v0.4.4 — 地下城流水线骨架 + 补充刀装 / 重伤停止机制 + 选项前缀统一（2026-06-18）

### 地下城活动流水线骨架
- 新建 `Underground.json`，97 节点，前缀 `U_`
- 主枢纽 20 个检测节点：公告弹窗、登录奖励、远征归来、行军中、战斗结果、刀剑掉落、特殊掉落、阵形选择、刀剑选择、部队选择、合战场标签检测、活动页面、菜单目录、本丸亮度守卫、重伤修刀/停止、闪退恢复（三项）、兜底等待
- 活动入口双路径：`U_IsSortie` 检测合战场标签选中→点击活动入口，`U_IsEventPage` 检测地下城活动页→进入
- 四大子链：本丸导航（本丸→菜单→出阵→活动入口）、换队长（刀剑选择→点击队长 1-5→降序→确认升序）、修刀/重伤（重伤弹窗→选槽→加速→确认修复→回本丸）、补充刀装（出阵前弹窗→部队记录→使用记录→确认补给）
- 23 个节点默认 `enabled: false`，由 GUI 选项控制
- 待截模板图：`地下城.png`、`特殊掉落_地下城.png`；任务条目已临时隐藏

### 选项前缀统一
- 合战场 10 个选项改名加 `S_` 前缀：`S_重伤处理`、`S_选择部队`、`S_选择时代`、`S_选择地域`、`S_选择阵形`、`S_换队长`、`S_补充刀装`、`S_王点前撤退`、`S_八图道中撤退`、`S_同步远征`
- 江户城 3 个选项改名加 `EC_` 前缀：`EC_出阵队伍`、`EC_换队长`、`EC_补充门票`
- 地下城 6 个选项加 `U_` 前缀：`U_重伤处理`、`U_选择部队`、`U_选择阵形`、`U_换队长`、`U_补充刀装`、`U_同步远征`
- 所有 checkbox 选项补充 `label` 字段，GUI 显示干净中文不再显示前缀
- 江户城 `出阵队伍` pipeline_override 节点 `SelectTeam` → `EC_SelectTeam`，`补充门票` 节点名补 `EC_` 前缀，修复之前 override 指向无效节点的问题

### 补充刀装检测位置优化
- `CheckEquipmentPopup` 从 `PostSortieHub` 移至 `IsTeamSelect` 的 next 列表首位
- 在部队选择页面首先检测刀装缺失弹窗，命中则处理补给再继续，未命中则正常选队
- 新增 `StopOnEquipmentPopup`（永远启用），排在 `CheckEquipmentPopup` 之后
- 勾选补充刀装时：弹窗出现→自动补给；不勾选时：弹窗出现→DoNothing→任务终止（刀装不满不安全）
- 点击"整备刀装"后新增 `post_delay: 500ms`

### 重伤停止机制
- `IsPreDamage1`（模板 `重伤.png`）和 `IsPreDamage2`（OCR "重伤"）改为默认 `enabled: false`
- 新增 `StopOnDamagePopup`（模板匹配）和 `StopOnDamageText`（OCR），默认 `enabled: false`
- 重伤处理下拉框修刀：启用 `IsPreDamage1/2` + `IsRepair`，禁用 stop 节点
- 重伤处理下拉框停止：启用 `StopOnDamagePopup/Text` + `IsStopOnDamage`，禁用处理节点
- 选停止时检测到重伤弹窗即终止任务，弹窗留在屏幕上便于查看

### 全局延迟调整
- `default_pipeline.json`：`pre_delay`/`post_delay` 100→150ms

### 清理
- 删除合战场和地下城的刀装补给死代码（`IsTroopDepleted` / `RefillTroops` / `ConfirmRefill`，战斗中无法补刀装且从未被激活）
- 清理 debug 目录 6/14-6/16 旧日志（17 个文件，释放约 2G）

### 回到本丸任务
- 新建 `GoHome.json`，16 节点，前缀 `G_`
- 主枢纽 9 个检测节点：公告弹窗、登录奖励、待关闭页面（OCR"×" `[1230,11,41,38]`）、本丸（`本丸.png` → DoNothing → 结束任务）、闪退恢复（三项）、右上角目录兜底（OCR"目录"→点击→验证菜单→点击本丸按钮→回到主枢纽）
- 待关闭页面链路：OCR"×"→点击关闭→OCR"目录"右上角→点击→验证菜单目录→点击本丸按钮
- 无自定义选项，勾选即运行，适合任务卡住时手动恢复

## v0.4.3 — 队伍1远征修复 + 菜单/面板兜底 + 同步远征配置共享（2026-06-14，Release 更新 2026-06-15）

### 远征队伍1接入
- `E_CheckTeam1` 从未被任何子枢纽引用，GUI 设置无法生效
- 新增 `E_ExpSubHub1`，next `[E_CheckTeam1, E_ExpSubHub2]`，与其他队伍结构一致
- `E_CheckTeam1` 默认 `enabled: false`，interface.json 中 20 个地图选项显式 `enabled: true`
- `E_CheckTeamStatus`、`S_IsTeamStatusPanel`、`E_IsTeamStatusPanel` 统一走 `E_ExpSubHub1`

### E_SelectTeam1-5 盲点改模板匹配
- 五个队伍选择节点从盲点 Click `[378,53,27,77]` 改为模板匹配 `本丸远征.png`（ROI `[374,42,39,108]`）+ Click

### 主枢纽菜单目录兜底
- 四条流水线（Sortie/Expedition/FlowerBrush/EdoCastle）主枢纽新增 `IsMenuDirectory` 节点
- 模板匹配 `菜单目录.png`（ROI `[581,28,112,57]`），解决菜单打开动画时序竞态导致的卡死
- Sortie/FlowerBrush/EdoCastle 命中后点"出阵"，Expedition 命中后点"本丸"

### 主枢纽队伍状况面板兜底
- Sortie/Expedition 主枢纽新增 `IsTeamStatusPanel` 节点
- 模板匹配 `本丸远征.png`，命中后跳 `E_ExpSubHub1`，面板已打开时跳过 OCR "状"点击

### 合战场同步远征配置共享（GUI 源码）
- `MaaProcessor.CreateNodeAndParam` 新增第 5 步：Sortie 任务开启同步远征时，从 `InstanceConfiguration` 读取远征任务的部队一~五配置并入 pipeline_override
- 解决合战场任务独立运行时无法获取远征队伍设置的 bug

### 全局延迟调整
- `default_pipeline.json`：`pre_delay`/`post_delay` 200→100ms

### 发布脚本
- `pack.sh` 排除列表新增 `pack.sh` 自身和 `agent/`
- `.gitignore` 新增 `agent/`

### 远征归来节点统一
- 五条流水线 8 个远征归来/战斗结果 OCR "经验" 节点的点击 target 统一为 `[388, 59, 54, 25]`（与 OCR ROI 一致），不再点击右下角盲点
- 江户城 `EC_HasExpeditionReturn` 从 TemplateMatch `远征结果.png` 改为 OCR "经验"，与其他流水线保持一致

### 本丸亮度守卫（ColorMatch）
- 五条流水线 6 个本丸模板匹配节点全部加装 ColorMatch 前置守卫
- 守卫 ROI `[66, 58, 11, 13]`，lower `[23, 95, 15]`，upper `[40, 155, 30]`
- 当公告/登录奖励等弹窗出现导致背景变暗时，守卫拒绝识别本丸，防止弹窗期间误触发
- 新增节点：`S_CheckHomeBrightness`、`E_CheckHomeBrightness`、`EC_CheckHomeBrightness`、`FB_CheckHomeBrightness`、`FB_CheckPostHomeBright`、`D_CheckHomeBrightness`

### 同步远征死锁修复
- 远征归来动画期间，本丸标志先出现、遮罩弹窗后出现，导致 `S_IsHome` 误匹配后卡在 `E_CheckTeamStatus`
- 修复：`S_IsHome` 的同步远征 override 从直连 `E_CheckTeamStatus` 改为走远征入口 `Expedition`，利用远征枢纽自身的检测顺序（先检测远征归来再检测本丸）处理动画竞态

### GUI 修复
- `ShouldMinimizeToTray` 设 false 后重启仍生效的 bug：`JsonHelper.cs` 序列化设置 `DefaultValueHandling.Ignore` 导致 `false` 值被跳过写入，旧 `true` 残留在文件中
- 修复：`DefaultValueHandling.Ignore` → `DefaultValueHandling.Include`，重新编译 AATL.exe

### 重伤检测与出阵确认优化
- `S_IsPreDamage1` 从 OCR "重伤" 改为模板匹配 `重伤.png`（ROI `[233, 128, 71, 549]`，阈值 0.98），识别更稳定，命中后点击关闭弹窗再走修刀流程
- `S_IsPreSortieConfirm` 从 TemplateMatch `即刻出阵.png` 改为 OCR "即刻"（ROI `[1156, 584, 96, 47]`），灰色按钮（队长重伤）时仍能识别并点击，触发重伤弹窗后被 `S_IsPreDamage1` 捕获进入修刀
- 解决了灰色即刻出阵按钮 + 修刀弹窗的死循环：之前模板匹配灰色按钮得分 0.93 低于阈值 0.98 导致卡在 CaptainHub，OCR 可稳定识别并进入修刀链路

### 合战场同步远征目录卡死修复（2026-06-15）
- 同步远征选项的 `E_ClickBackToHome` 覆盖缺失 `action` 和 `on_error`，点击目录"本丸"后 `S_WaitRefresh` 检测失败时原地重试 20s 超时
- 修复：补回 Click action `[1037, 169, 110, 44]`，新增 `on_error: S_DetectWhereAmI` 兜底跳转主枢纽重新定位
- 发布包新增 `config/maa_option.json`，`save_on_error` 默认 `false` 关闭错误截图

### 登录奖励 Click3 缺少 on_error 导致任务超时终止（2026-06-16）
- `*_LoginRewardClick3` 点击关闭第三个弹窗后 next 指向 `*_IsLoginReward` 检测是否还有堆叠弹窗
- 当登录奖励全部关闭后 `*_IsLoginReward` 识别失败，但作为 next 列表成员失败时，框架查找的是上级节点 `*_LoginRewardClick3` 的 `on_error`
- 四个 `LoginRewardClick3` 节点均无 `on_error`，框架无路可走原地重试直到 20s 超时 → `Task.Failed`
- 修复：四条流水线的 `LoginRewardClick3` 统一补充 `on_error`：
  - `S_LoginRewardClick3` → `S_DetectWhereAmI`
  - `FB_LoginRewardClick3` → `FB_DetectWhereAmI`
  - `E_LoginRewardClick3` → `Expedition`
  - `EC_LoginRewardClick3` → `EC_DetectWhereAmI`

## v0.4.2 — 刀解流水线 + 前缀统一 + 公告/登录奖励/闪退恢复（2026-06-13）

### 刀解流水线 (Disassemble.json)
- 新建 `D_` 前缀流水线，30 个节点，pre_delay 统一 300ms
- GUI 7 个刀种复选框（短/胁/打/太/大太/枪/薙），pipeline_override 按勾选启用对应筛选节点
- 主枢纽检测 5 种界面：本丸、菜单目录、锻刀、刀解、筛选、邮箱
- 解体循环：一键选择 → 刀解确认 → 解体 → 再次选择，直到无可解体刀剑 → 进邮箱
- 邮箱链路：领取 → 奖励确认 → 购买详情 → 关闭邮箱
- 筛选共用：刀解和邮箱各走同一套筛选节点
- 该功能尚不稳定，可能中途停止任务

### 远征前缀统一
- 49 个节点统一 E_ 前缀（原 Expedition_ 和无名节点全部改名）
- ClickExpeditionReturn 修复：next 改回 Expedition 主枢纽，避免多远征归来死循环
- 远征坐标微调

### 合战场前缀统一
- Sortie_ 统一改为 S_ 短前缀，与其他三条流水线一致

### 全局
- 公告弹窗节点 next 全部统一为自引用
- 登录奖励节点（四条流水线，模板匹配"登录奖励.png"，三击关闭，间隔 300ms）
- 一键启动配置：标签"游戏路径"→"模拟器路径"，README 新增使用说明
- interface.json 全量引用同步
- 移除 Python 层（aatl/、venv/、setup.bat、requirements.txt）
- 远征地图坐标全部由 interface.json pipeline_override 管理，即改即生效，无需重启 GUI
- 全部流水线 167 个 ROI 四周各扩 5px，提升跨设备兼容性
- 公告弹窗 ROI 扩至 [1061, 468, 53, 183] + 阈值降至 0.95
- 游戏图标阈值降至 0.95，修复挪动图标后识别不到
- 重新编译 AATL.exe，发布资源同步更新

### 游戏闪退自动恢复
- 四条流水线主枢纽新增游戏恢复节点（每条独立前缀避免命名冲突：S_/E_/EC_/FB_）
- 全屏模板匹配游戏图标 → 点击 → 等 3s
- 模板匹配登录按钮 [670, 534, 114, 68] → 点击 → 等 3s
- 模板匹配游戏更新弹窗 [448, 436, 110, 36] → 点击关闭
- 游戏闪退回到桌面后自动重新进入，无需人工干预

## v0.4.1 — 公告弹窗 + 江户城前缀补完 + 王点撤退补完 + 八图道中撤退（2026-06-13）

### 公告弹窗检测
- 四条流水线主枢纽首位新增公告弹窗节点（E_/S_/EC_/FB_IsAnnouncementPopup）
- ROI [1066, 473, 43, 173] 模板匹配"公告弹窗.png"，green_mask + threshold [0.98]
- 命中后点击关闭并返回主枢纽重新扫描，连续多个公告弹窗逐个关闭，全部关完后顺延后续节点

### 江户城全局前缀补完
- 批量重命名 55 个旧节点加 EC_ 前缀，与其他三条流水线命名规范统一
- 仅保留入口节点 EdoCastle 无前缀

### 王点前撤退补完
- E7 R1-R3 从 ColorMatch 占位改为 TemplateMatch
- E8 R1-R4 从 ColorMatch 占位改为 TemplateMatch
- 删除 E5_R2 和 E7_R4 无效 ColorMatch 占位节点

### 八图道中撤退
- 合战场新增 checkbox"八图道中撤退"，独立于王点前撤退
- 仅适配时代八四条路线（R1-R4），行军后先检测道中再检测王点

## v0.4.0 — 合战场流水线优化（2026-06-12）

### 远征派遣修复
- SelectTeamBtn 缺少 post_delay：点完部队标签后 UI 动画未完成就点远征开始，按钮仍为灰色导致弹窗不出
- VerifyConfirmScreen 从 OCR "部队选择" 改为 OCR "远征确认"（ROI [577, 79, 127, 35]），直接检测弹窗
- ConfirmExpedition 确认点击坐标更新为 [783, 570]
- 新增独立的 VerifyTeamSelect 恢复节点：弹窗未识别到 → OCR "部队选择" → 在选队页则重试 StartExpedition → 不在则回 DetectExpeditionScreen
- ClickDirectoryTop pre_delay 从 100ms 改为 3000ms，等菜单动画完成再点目录

### 全局点击延迟
- 全部 103 个 Click 节点统一加 pre_delay: 100，消除点击时序问题
- 所有 FallbackWait 节点去掉 post_delay

### 刷花流水线
- FB_PostFallbackClick 盲点改为 FB_PostFallbackWait（DoNothing）
- 远征结果和战斗结果检测从 TemplateMatch 改为 OCR（ROI [388, 59, 54, 25], expected "经验"），与合战场统一
- 主枢纽和行军后枢纽共四个节点统一改造

### 合战场地域选择
- IsRegionSelect 新增 Sortie_VerifyRegionSelect 双重确认节点：连续两次识别到"地域选择"才点击，防止动画过渡期间误触刀男弹窗
- ClickRegion pre_delay 从 100ms 提升至 500ms

### 回本丸流程
- ClickBackToHome pre_delay 保持 100ms
- 确认远征后 4s → ClickDirectoryTop 3s → 菜单目录验证 → 回本丸 → 60s 等待刷新

### 修刀加速符简化
- Sortie_MatchAccelUnused / Sortie_MatchAccelUsed 两个模板匹配节点废弃，改为 Sortie_ClickAccel 直接点击
- 删除未使用加速符.png 和使用加速符.png 模板图片

### 地域选择双重确认
- IsRegionSelect 新增 Sortie_VerifyRegionSelect 二次确认，连续两次识别到才点击，防止动画过渡误触
- IsRegionSelect 新增 on_error 回主枢纽，避免 VerifyRegionSelect 失败时死循环
- ClickRegion 新增 post_delay 800ms，等游戏过渡到部队选择页面后再回主枢纽扫描

### 六图王点
- E6 R1~R4 全部从 ColorMatch 改为 TemplateMatch
- 模板图片统一命名：王点_6_1_1.png / 王点_6_2.png / 王点_6_3.png / 王点_6_4.png

## v0.3.5 — 流水线去冗余 + 兜底机制重构 + 检非违使检测（2026-06-11）

### 合战场去冗余
- 去除所有保底盲点击节点（FallbackClick、FallbackRetry、DetectWhereAmI_Round2、PostFallbackClick）
- 去除第二主枢纽（DetectAfterMarching 及 8 个 Post* 检测/点击节点），PostClickMarching 改指回主枢纽
- 节点数从 ~120 精简至 111

### 江户城去冗余
- 去除保底盲点击节点（EC_FallbackClick、EC_FallbackRetry）

### 刷花去冗余
- 去除保底盲点击节点（FB_FallbackClick），节点数减至 50

### 新增检非违使检测
- 主枢纽新增 Sortie_IsKebiishi 节点：ROI [527, 98, 69, 54] 模板匹配"检非.png"，green_mask + threshold [0.98]
- 行军后节点 CheckBossBeforeMarch 同步新增，排在重伤检测之前
- 命中后点击关闭并回到主枢纽

### 兜底机制重构
- 所有保底盲点击替换为 FallbackWait（DoNothing + 无延迟），仅保持流水线存活不误触
- 四项管道主枢纽全部加装：Sortie_FallbackWait、EC_FallbackWait、FB_FallbackWait、Expedition_FallbackWait
- Sortie_DetectAfterSortieNow 二级枢纽加装 FallbackWait，修复出阵后动画帧全失败导致 20s 超时终止

### 修复
- 远战动画帧无匹配节点导致主枢纽全部识别失败后框架自动终止（FallbackWait 兜底解决）
- 出阵后枢纽无兜底导致 Task timeout 20079ms → Task.Failed（FallbackWait 兜底解决）

### MaaFramework 全局节点命名
- 发现 MaaFramework 加载时所有流水线共享节点命名空间，同名节点会导致资源加载失败
- 每条流水线加独立前缀：S_（合战场）、E_（远征）、EC_（江户城）、FB_（刷花）

## v0.3.4 — 江户城优化与收尾（2026-06-11）

### 江户城优化
- 移除战斗结果检测（IsBattleResult / PostBossBattleResult），江户城不需要
- 打完一轮后不再回本丸，直接终止任务由 GUI repeat_count 重启下一轮
- IsMapPointSelect 从 OCR 改为模板匹配"江户城地图点选择.png"
- 难度硬编码为超难，移除难度选择下拉框
- 保底点击改为无点击循环（EC_FallbackRetry），避免误触菜单

### 修复
- 刷花任务进入部队选择页后跳过选部队一直接点刀剑槽位：FlowerBrush.json 新增 FB_ClickTeam1 节点
- 合战场关闭同步远征后该功能依旧生效：Sortie.json 中 Sortie_IsHome 默认 next 从 CheckTeamStatus 改为 Sortie_NavigateToSortie，interface.json 同步远征 checkbox 的 pipeline_override 新增 Sortie_IsHome 重定向

## v0.3.3 — 合战场通用流水线（2026-06-10）

### 合战场完整功能
- Sortie.json 通用合战场流水线（100+ 节点）：支持自选时代/地域/阵形/部队的参数化出阵
- 双轮主枢纽 + 行军后枢纽 + 即刻出阵后枢纽四枢纽架构，避免动画帧任务终止
- 王点前撤退功能：四图模板匹配 + 阈值 [0.98]，行军前先检测再决定点不点
- 重伤检测双时机（出阵前/行军中），修刀全链路：目录→修复→选槽→加速符循环→确认→出阵
- 重伤处理下拉框（修刀/停止）互斥二选一
- 加速符匹配循环：反复点未使用直到匹配已使用

### 补充刀装
- 新增出阵后刀装补充弹窗检测（模板匹配"整备刀装.png"），仅在弹窗出现时触发补给
- 补给链路：CheckEquipmentPopup → PreCheckTroopRecord → PreCheckUseRecord → PreConfirmSupply → IsPreSortieConfirm

### 同步远征
- 合战场回本丸后自动检测队伍空闲并派遣远征，完全复用远征流水线的队伍检测和派遣链路
- IsHome → CheckTeamStatus（远征）→ 检测空闲 → 派遣 → GoHome → CheckTeamStatus（循环）
- AllTeamsBusy 重定向至 NavigateToSortie 回到合战场出阵
- 仅通过 pipeline_override 重定向 3 个关键节点流向

### 性能优化
- 大量识别节点从 OCR 改为模板匹配，消除 OCR 延迟与误判
- 所有模板匹配统一启用 green_mask + threshold [0.98]
- 所有枢纽节点增加 pre_delay: 0，消除不必要等待

### 菜单流程优化
- 新增菜单进入前目录验证（模板匹配"菜单目录.png"），防止未进入菜单就点击按钮
- 点击菜单按钮后移除目录验证直接走枢纽，避免时代选择界面误匹配导致 20 秒死循环
- 四项流水线目录验证全部统一为模板匹配

### 远征修复
- CheckTeamStatus 添加 300ms post_delay 等队伍列表刷新动画
- ConfirmExpedition 添加 4 秒 post_delay 等远征派遣动画
- 批量 OCR 检测修复：嵌套子枢纽防止框架合并，每队独立 OCR
- 队伍检测 ROI 精确调整

## v0.3.2 — 刷花任务 + 发布打包（2026-06-09）

### 刷花流水线
- FlowerBrush.json 刷花流水线：仅部队一单骑出阵 1-1，自动循环进入合战场刷花

### 发布打包
- 发布打包脚本 pack.sh：自动排除仅开发用文件
- GitHub Release v0.3.2 含发行版 ZIP（x86_64 Windows，基于 MuMu 模拟器 12）
- .gitignore 更新：排除 Release ZIP 归档文件

## v0.3.1 — 江户城活动自动化（2026-06-08）

### 江户城流水线
- EdoCastle.json 完整流水线（约 50 节点）：导航 → 选难度 → 部队选择 → 地图 4 点路线 → 王点 → 战斗结算 → 掉落检测 → 回本丸 → 终止
- DetectWhereAmI 十路统一界面判断，可复用至所有活动任务
- 换队长条件启用：筛选装备马的刀剑男士，选疲劳值最低者任队长（控花机制）
- 门票补充条件启用：自动检测通行令牌耗尽时补充 3 枚
- 地图路线 4 点模板匹配，先进点优先检测避免误触
- PostBossDetect 四路退出循环：战斗结算 / 特殊掉落 / 等待 / 活动页终止

## v0.3 — 远征自动化（2026-06-08）

### 远征任务全流程
- 远征任务全流程自动化：本丸检测 → 队伍状态检查 → 空闲队伍派遣 → GoHome 回城 → 循环
- 三路并行入口检测：远征归来收取、远征界面脱困、本丸主页识别
- 部队一~五独立配置远征地图（20 张可选 + 休息），休息队伍自动跳过
- 可自定义刷新间隔
- 每次点击加入随机位置偏移，模拟真实操作
- 启动时自动生成流水线坐标，修改配置后重启即生效

### GUI
- 基于 MFAAvalonia v2.12.1 源码构建，项目名为 AATL.exe
- 自定义程序图标
- 最小化到系统托盘
- 设置面板：5 个部队下拉框 + 刷新间隔输入框
- 所有节点失败时自动回退到起始循环，不会卡死

### 项目结构
- 扁平化根目录，双击 AATL.exe 即用
- 移除 MFAToolsPlus（独立工具，不随项目分发）
- 新增 default_pipeline.json 全局配置
- 流水线迁移至嵌套格式（MaaFramework 新版）
